export function hello(name, greeting) { return `${greeting} ${name}`; }
export function greet(title, name) { return `${title} ${name}`; }
export class Greeter { constructor(prefix) { this.prefix = prefix; } }
